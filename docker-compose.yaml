name: GakuToolkit

services:
  gakutoolkit:
    image: ghcr.io/pinisok/gakutoolkit:latest
    volumes:
      - ./cache:/app/GakuToolkit/cache:rw
      - /home/gpy/.ssh:/root/.ssh:rw
      - ./drive:/app/GakuToolkit/res/drive:rw
    environment:
      GIT_EMAIL: prain3ps@gmail.com
      GIT_NAME: pinisok
      REMOTE_NAME: gakumas
      RCLONE_CONFIG: /app/GakuToolkit/cache/rclone.conf